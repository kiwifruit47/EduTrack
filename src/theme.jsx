import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "##507DBC",
            dark: "#0D1321",
            contrastText: "#F1F2EE",
        }
    }
});

export default theme;