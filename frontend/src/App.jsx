import AppRouter from './router/AppRouter';

function App() {
  // Initialize the root component of the EduTrack application.

  return (
        // Render the application's routing layer and top-level providers.
        <AppRouter/>
  )
}

export default App
